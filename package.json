{
  "name": "cape-fear-stone-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && python3 -m http.server 5173 --bind 127.0.0.1 --directory dist",
    "build": "node scripts/build.mjs",
    "preview": "python3 -m http.server 4173 --bind 127.0.0.1 --directory dist"
  }
}
